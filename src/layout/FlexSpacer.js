import React from 'react'
import styled from 'styled-components'

const SpacerWrap = styled.div`
.flex-spacer {
  margin: auto;
}
`

const FlexSpacer = ({className, id}) => { 
    const classes = ["wmk-layout"];
    classes.push()
    return (<div className="flex-spacer" id={id} />)}