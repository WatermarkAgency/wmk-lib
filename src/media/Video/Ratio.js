import React from "react";

export default ({ width, height }) => (
  <svg
    version="1.1"
    x="0px"
    y="0px"
    viewBox="0 0 16 9"
    style={{ "enable-background": `new 0 0 ${width} ${height}` }}
  ></svg>
);
